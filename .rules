## Writing style

* Use Semantic Line Breaks, but don't be too aggressive adding link breaks.
* Markdown heading should have newlines around
* Lists should have a newline before the first item and after the last item
